---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { resume } from "../../content/resume";

const companySpan = (group: (typeof resume.experience)[number]) => {
  const first = group.roles[0];
  const last = group.roles[group.roles.length - 1];
  return `${last.start} – ${first.end}`;
};
---

<BaseLayout
  title={`${resume.name} — Resume`}
  description={`${resume.name} · ${resume.positioning}`}
  path="/resume/print"
  noIndex
>
  <article class="doc">
    <header class="doc__header">
      <div class="doc__ident">
        <h1 class="doc__name">{resume.name}</h1>
        <p class="doc__positioning">{resume.positioning}</p>
      </div>
      <div class="doc__contact">
        <p>{resume.location}</p>
        <p>
          <a href={`mailto:${resume.email}`}>{resume.email}</a>
        </p>
        <p>
          <a href={resume.links.site.href}>{resume.links.site.label}</a>
          <span class="sep">·</span>
          <a href={resume.links.linkedin.href}>LinkedIn</a>
          <span class="sep">·</span>
          <a href={resume.links.github.href}>GitHub</a>
        </p>
      </div>
    </header>

    <hr class="doc__rule" />

    <section class="doc__summary">
      {resume.summary.map((p) => <p>{p}</p>)}
    </section>

    <section class="doc__section">
      <h2 class="doc__h">Experience</h2>
      <ol class="doc__companies">
        {resume.experience.map((group) => (
          <li class="doc__company">
            <header class="doc__company-header">
              <span class="doc__company-name">{group.company}</span>
              <span class="doc__company-span">{companySpan(group)}</span>
            </header>
            <ol class="doc__roles">
              {group.roles.map((role) => (
                <li class="doc__role">
                  <header class="doc__role-header">
                    <p class="doc__role-title">
                      <strong>{role.title}</strong>
                    </p>
                    <span class="doc__role-dates">{role.start} – {role.end}</span>
                  </header>
                  <ul class="doc__bullets">
                    {role.bullets.map((b) => <li>{b}</li>)}
                  </ul>
                </li>
              ))}
            </ol>
          </li>
        ))}
      </ol>
    </section>

    <section class="doc__section doc__section--split">
      <div>
        <h2 class="doc__h">Education</h2>
        <p class="doc__edu">
          <strong>{resume.education.school}</strong><br />
          {resume.education.degree}
          {resume.education.detail && <><br /><em>{resume.education.detail}</em></>}
        </p>
      </div>
      <div>
        <h2 class="doc__h">Credentials</h2>
        <ul class="doc__creds">
          {resume.credentials.map((c) => <li>{c}</li>)}
        </ul>
      </div>
    </section>

    <footer class="doc__foot">
      <span>{resume.links.site.label}</span>
      <span class="sep">·</span>
      <span>{resume.links.ambr.label}</span>
      <span class="sep">·</span>
      <span>{resume.email}</span>
    </footer>
  </article>
</BaseLayout>

<style is:global>
  @page {
    size: Letter;
    margin: 0.55in 0.6in;
  }

  html,
  body {
    background: #ffffff !important;
  }

  body {
    color: #15140f;
    font-family: var(--f-body);
    font-size: 10pt;
    line-height: 1.42;
  }

  /* Suppress nav/footer chrome from other layouts if it leaks in */
  header.masthead,
  footer.footer {
    display: none !important;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  .doc {
    max-width: 100%;
    padding: 0;
    background: #ffffff;
  }

  .doc__header {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: end;
    gap: 1rem;
    padding-bottom: 0.5rem;
  }
  .doc__name {
    font-family: var(--f-display);
    font-size: 32pt;
    line-height: 1;
    letter-spacing: 0;
    color: #15140f;
  }
  .doc__positioning {
    font-family: var(--f-body);
    font-size: 11pt;
    color: #2f4a3a;
    margin-top: 0.4rem;
    font-weight: 600;
  }
  .doc__contact {
    text-align: right;
    font-size: 9pt;
    color: #3a3833;
    line-height: 1.5;
  }
  .doc__contact .sep {
    margin: 0 0.3rem;
    color: #8a847a;
  }

  .doc__rule {
    border-top: 1.5px solid #2f4a3a;
    margin: 0.4rem 0 0.85rem;
  }

  .doc__summary {
    display: grid;
    gap: 0.45rem;
    margin-bottom: 0.85rem;
    max-width: 88%;
  }
  .doc__summary p {
    font-family: var(--f-display);
    font-size: 11.5pt;
    line-height: 1.42;
    color: #2a2925;
  }
  .doc__summary p:first-child {
    color: #15140f;
  }

  .doc__section {
    margin-top: 0.75rem;
  }
  .doc__section--split {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 1.5rem;
    margin-top: 0.9rem;
  }

  .doc__h {
    font-family: var(--f-body);
    font-size: 11.5pt;
    font-weight: 700;
    color: #15140f;
    padding-bottom: 0.25rem;
    margin-bottom: 0.55rem;
    border-bottom: 1px solid #2f4a3a;
  }

  .doc__companies {
    display: grid;
    gap: 0.7rem;
    padding: 0;
    list-style: none;
    margin: 0;
  }
  .doc__company {
    break-inside: avoid-page;
    page-break-inside: avoid;
  }
  .doc__company-header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.6rem;
    padding-bottom: 0.15rem;
    margin-bottom: 0.4rem;
    border-bottom: 0.5px solid #e0d9c8;
    break-after: avoid;
    page-break-after: avoid;
  }
  .doc__company-name {
    font-family: var(--f-display);
    font-size: 14pt;
    line-height: 1;
    color: #15140f;
    letter-spacing: -0.005em;
  }
  .doc__company-span {
    font-size: 9pt;
    color: #5e5a54;
    font-variant-numeric: tabular-nums;
    white-space: nowrap;
  }

  .doc__roles {
    display: grid;
    gap: 0.5rem;
    padding: 0 0 0 0.75rem;
    list-style: none;
    margin: 0 0 0 0.05rem;
    border-left: 0.5px solid #d6ccb4;
  }
  .doc__role {
    break-inside: avoid;
    page-break-inside: avoid;
    position: relative;
  }
  .doc__role::before {
    content: "";
    position: absolute;
    left: calc(-0.75rem - 2.5px);
    top: 0.5em;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: #2f4a3a;
    box-shadow: 0 0 0 2.5px #ffffff;
  }
  .doc__role-header {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: baseline;
    gap: 0.6rem;
    margin-bottom: 0.2rem;
  }
  .doc__role-title {
    font-size: 10.5pt;
    color: #15140f;
  }
  .doc__role-title strong {
    font-weight: 600;
  }
  .doc__role-dates {
    font-size: 9pt;
    color: #5e5a54;
    font-variant-numeric: tabular-nums;
    white-space: nowrap;
  }
  .doc__bullets {
    margin: 0.15rem 0 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0.15rem;
  }
  .doc__bullets li {
    position: relative;
    padding-left: 0.85rem;
    font-size: 9.6pt;
    line-height: 1.44;
    color: #1f1e1a;
    break-inside: avoid;
  }
  .doc__bullets li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.58em;
    width: 5px;
    height: 1px;
    background: #2f4a3a;
  }

  .doc__edu {
    font-size: 10pt;
    line-height: 1.5;
    color: #1f1e1a;
  }
  .doc__edu strong {
    font-weight: 600;
  }
  .doc__edu em {
    color: #5e5a54;
    font-style: italic;
  }

  .doc__creds {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0.2rem;
  }
  .doc__creds li {
    position: relative;
    padding-left: 0.8rem;
    font-size: 10pt;
    color: #1f1e1a;
  }
  .doc__creds li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.65em;
    width: 5px;
    height: 1px;
    background: #2f4a3a;
  }

  .doc__foot {
    margin-top: 1.1rem;
    padding-top: 0.45rem;
    border-top: 0.5px solid #cfc6b4;
    font-size: 8.6pt;
    color: #5e5a54;
    text-align: center;
  }
  .doc__foot .sep {
    margin: 0 0.45rem;
    color: #8a847a;
  }
</style>
